// @ts-check

/** @type {import("prettier").Config} */
const config = {
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    printWidth: 120,
    tabWidth: 4,
    arrowParens: "avoid"
};

export default config;
